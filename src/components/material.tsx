import React from 'react';
import { Outlet, Link } from "react-router-dom";

type Props = { title:string, list: string[], select:number[] };
  
export function materialView({ title,  list, select }: Props) {
  return (
   <div>
        
   </div>
  );
}