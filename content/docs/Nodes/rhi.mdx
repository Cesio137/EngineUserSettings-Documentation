---
title: RHI
description: Vulkan, DirectX 11, DirectX 12
---

import { ImageZoom } from "fumadocs-ui/components/image-zoom";

## Set Prefered RHI

-   Change between DX11, DX12 or VULKAN.
-   OBS: Game need to be restarted to take effect.

<UFunction
    name="Set Prefered RHI"
    pure={false}
    inPins={[
        { name: "", pintype: "exec" },
        { name: "Graphics RHI", pintype: "byte" }
    ]}
    outPins={[{ name: "", pintype: "exec" }]}
/>

## Get Prefered RHI

-   Gets the RHI that will be used when the game is restarted.

<UFunction
    name="Get Prefered RHI"
    pure={true}
    outPins={[{ name: "Return Value", pintype: "byte" }]}
/>

## Get Current RHI

-   Gets the RHI that will be used when the game is restarted.

<UFunction
    name="Get Current RHI"
    pure={true}
    outPins={[{ name: "Return Value", pintype: "byte" }]}
/>
